import { ModuleStub } from "@/components/portal/module-stub";

export default function MachinesAssets() {
  return (
    <ModuleStub
      title="Machines & Assets"
      description="Machine registry with per-machine performance averages, uptime and output."
      scope="Full + configuration (system-wide)"
    />
  );
}
